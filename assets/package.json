{
  "name": "rover-openlayers",
  "version": "0.1.0",
  "description": "Build tooling for the Rover Elixir library. Not published to npm.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "test": "node --test \"test/**/*.test.js\"",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "esbuild": "^0.28.2",
    "ol": "^10.4.0",
    "ol-mapbox-style": "^13.4.3"
  }
}
